# kotlin.language

> **GENERATED FILE — DO NOT HAND-EDIT.** Regenerated from the live skill
> graph by the `skills-catalog` generator. Edit the skill via CLI/REST/MCP
> (see `docs/scripts/` / `docs/API.md`) — this file will be overwritten.

<!-- skills-catalog:section=header -->
## Header

- **Name:** kotlin.language
- **Title:** Kotlin Programming Language
- **Version:** 0.1.0
- **Kind:** atomic
- **Status:** draft
- **Domain:** language
- **Complexity:** intermediate
- **Tags:** android, coroutines, jvm, kotlin, language

<!-- /skills-catalog:section=header -->
<!-- skills-catalog:section=description -->
## Description

Reference for Kotlin: null safety, coroutines, Java interop, and its status as Android's preferred language since 2019.

<!-- /skills-catalog:section=description -->
<!-- skills-catalog:section=dependencies -->
## Dependencies

### Requires

- [`java.language`](java_language.md)

<!-- /skills-catalog:section=dependencies -->
<!-- skills-catalog:section=dependents -->
## Dependents

- [`android.overview`](android_overview.md)

<!-- /skills-catalog:section=dependents -->
<!-- skills-catalog:section=resources -->
## Resources

| Title | URL | Type |
|---|---|---|
| Kotlin Documentation | https://kotlinlang.org/docs/home.html | official-doc |
| Kotlin Language Specification | https://kotlinlang.org/spec/introduction.html | official-doc |

<!-- /skills-catalog:section=resources -->
<!-- skills-catalog:section=content -->
## Content

---

# Kotlin

## Overview
Kotlin is a statically-typed, JVM-first language (also compiling to native
code and JavaScript) created by JetBrains and open-sourced in 2012. Google
announced first-class support for Android development in 2017, and Kotlin
became Android's preferred language in 2019, fully interoperable with
existing Java code and libraries.

## Key facts
- Null safety is built into the type system: types are non-nullable by
  default; nullable types are marked explicitly (`String?`), eliminating a
  large class of `NullPointerException`s at compile time.
- Concise syntax: type inference, data classes (auto-generated
  `equals`/`hashCode`/`toString`/`copy`), extension functions, and
  default/named arguments substantially reduce boilerplate versus Java.
- Coroutines (`kotlinx.coroutines`) provide structured concurrency and
  `suspend` functions as the idiomatic alternative to callback- or
  `Future`-based async code, and are the standard approach for asynchronous
  work on Android.
- 100% Java interoperability: Kotlin classes can extend Java classes and
  vice versa within the same project; both compile to JVM bytecode (or DEX
  bytecode via D8/R8 on Android).
- Kotlin Multiplatform (KMP) allows sharing business logic across JVM,
  Android, iOS (via Kotlin/Native), and web targets from one codebase.
- Official build support via Gradle's Kotlin DSL (`build.gradle.kts`) and
  the `kotlin-android` / `kotlin-multiplatform` Gradle plugins.

## Why it matters here
Kotlin requires Java knowledge (shared JVM semantics and interop) and is one
of the six technologies named in the R6 canonical wizard use case alongside
Android, Java, C++, and CMake.

<!-- /skills-catalog:section=content -->
<!-- skills-catalog:section=footer -->
## Footer

- _Generated by `skills-catalog/v3` from roster fingerprint `60a626fb9d64`._
<!-- /skills-catalog:section=footer -->

## Usage

### Typical invocation

This skill is loaded when the agent encounters Kotlin-related work:

- Writing or reviewing Kotlin source (data classes, coroutines, extension functions)
- Building Android apps with Kotlin-first Jetpack Compose
- Configuring Kotlin Gradle DSL (`build.gradle.kts`)
- Investigating Kotlin Multiplatform (KMP) shared logic

```
# The skill is auto-loaded as a prerequisite for Android work.
# Explicit invocation:
TASK: convert this Java utility class to idiomatic Kotlin with null safety
```

### Build tool reference

```bash
# Kotlin Gradle DSL
./gradlew build

# Kotlin compiler directly
kotlinc src/main.kt -include-runtime -d app.jar
```

## Constitution References

| Reference | Meaning |
|---|---|
| **§11.4.17** | Universal-vs-project classification. Kotlin is a universal language skill — the consuming project supplies its concrete Kotlin version, coroutine dispatcher, and KMP targets. |
| **§11.4.35** | Canonical-root inheritance clarity. The skill defines general Kotlin knowledge; the project declares its Kotlin version and target platforms. |

## Cross-links

- **Requires:** [`java.language`](java_language.md)
- **Dependent:** [`android.overview`](android_overview.md)
- **Parent domain:** [`language`](../by-domain/language.md)
- **R6 canonical wizard set:** kotlin is one of the six technologies

## Integration

| Surface | How it hooks in |
|---|---|
| **JVM/DEX runtime** | Kotlin compiles to JVM bytecode (server) or DEX bytecode (Android via D8/R8). 100% Java interoperability — Kotlin classes can extend Java classes and vice versa. |
| **Coroutines** | `kotlinx.coroutines` provides structured concurrency and `suspend` functions. The idiomatic async approach on Android, replacing callbacks/Futures. |
| **Gradle Kotlin DSL** | `build.gradle.kts` is the Kotlin-first Gradle configuration format. `kotlin-android` and `kotlin-multiplatform` plugins. |
| **Kotlin Multiplatform** | Share business logic across JVM, Android, iOS (Kotlin/Native), and web from one codebase. |

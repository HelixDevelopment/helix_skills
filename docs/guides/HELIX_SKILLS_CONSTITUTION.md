# Helix Skills Constitution

This constitution **extends** the Helix Universal Constitution at
`constitution/Constitution.md`. All clauses there apply unless explicitly
overridden below.

## Project Articles

_No project-specific articles at this time._ New articles are numbered
from §101 and must be genuinely project-specific (skills packaging, agent
wiring) — never a restatement or dilution of a universal clause.

## Overrides of Universal Constitution

_None._ (Every override MUST be justified explicitly under an
`Override §X.Y` heading here.)

## Owned-submodule set

Per Universal §4, the submodules this project owns:

```
(none — the only submodule is `constitution/`, which is the shared
Helix Constitution source, not a project-owned consuming submodule)
```

## Project-specific remotes

| Repo | Remotes |
|---|---|
| Main (`helix_skills`) | github, gitlab, gitflic, gitverse (origin fans out to all four) |
| `constitution/` | vendored submodule; upstreams managed in the HelixConstitution repo itself |

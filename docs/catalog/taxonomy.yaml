# catalog-engine taxonomy — helix_skills PROJECT INJECTION (§11.4.28).
# Lightweight taxonomy for a constitution/skills project (no hardware clusters).

clusters:
  - governance
  - action_prefix
  - token_efficiency
  - testing
  - hooks
  - meta-test-mutation
  - multitrack
  - workable_items

keyword_cluster:
  constitution: governance
  inheritance: governance
  propagation: governance
  covenant: governance
  governance: governance
  action_prefix: action_prefix
  prefix: action_prefix
  grammar: action_prefix
  subsystem: action_prefix
  token_efficiency: token_efficiency
  token: token_efficiency
  cache: token_efficiency
  testing: testing
  pre_build: testing
  meta_test: testing
  hook: hooks
  guard: hooks
  pretooluse: hooks
  branch_consistency: hooks
  track_branch: hooks
  work_track: hooks
  mutation: meta-test-mutation
  cm_: meta-test-mutation
  multitrack: multitrack
  conductor: multitrack
  ruler: multitrack
  alias_priority: multitrack
  fallback: multitrack
  bootstrap: multitrack
  host_budget: multitrack
  reconcile: multitrack
  supervisor: multitrack
  sessions: multitrack
  workable: workable_items
  items: workable_items
  atm: workable_items
  import: workable_items
  export: workable_items
  crud: workable_items
  diary: workable_items
  roundtrip: workable_items
  classify: workable_items
  assign: workable_items
  validate: workable_items
  obsolete: workable_items
  reopen: workable_items
  migrate: workable_items
  mutate: workable_items
  subtask: workable_items
  group: workable_items
  sync_diff: workable_items
  repair: workable_items
  gap: workable_items
  attribution: workable_items
